VITE_API_BASE=https://jsonplaceholder.typicode.com
VITE_ENABLE_MOCKING=true

# HYBRID MODE — workshop Scenario 3.
# GET /users bypasses the worker and hits the real JSONPlaceholder
# API. Everything else (notably /properties) stays mocked.
VITE_MSW_OMIT_KEYS=GET_HOSTS
