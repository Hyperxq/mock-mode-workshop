VITE_API_BASE=https://jsonplaceholder.typicode.com
VITE_ENABLE_MOCKING=true

# ---------------------------------------------------------------
# HYBRID MODE — workshop Scenario 3
#
# Uncomment the line below to let GET /users (the "hosts" strip)
# pass through to the real JSONPlaceholder API while everything
# else stays mocked. Restart the dev server after changing this.
# ---------------------------------------------------------------
# VITE_MSW_OMIT_KEYS=GET_HOSTS
